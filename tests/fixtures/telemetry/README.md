# Telemetry Fixtures

Sample heartbeat and telemetry payloads used for manual debugging or future automated tests.

- `heartbeat.json` – Minimal heartbeat payload containing device RSSI and timestamp.
- `telemetry-sample.json` – Representative telemetry snapshot containing key metric fields.
