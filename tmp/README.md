# Temporary Artifacts

Captured logs and CLI outputs that inform operational runbooks and audits.

- `ci/` contains Vitest and CI transcripts referenced during compliance reviews.
- `wrangler/` stores Cloudflare CLI responses captured during worker debugging.
- `misc/` holds short-lived notes or placeholders kept for documentation completeness.
