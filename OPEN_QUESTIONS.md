# Open Questions

1. **SAFE metric scope:** Should `alerts` and `firmware_version_major_minor` graduate into the SAFE list? CN gateway + SDK owners still need a joint decision.
2. **Export pubkey rotation owner:** Who is accountable for rotating the Ed25519 verification key (and updating the Wrangler secret) every time CN gateway rotates its signer?
3. **Privacy notice refresh:** Legal/Comms still need to provide UTF-8 Mandarin copy so the operator notice matches the current architecture.
