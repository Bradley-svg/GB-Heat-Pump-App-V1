{
  "name": "gb-heat-pump-app-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create GREENBRO_DB",
    "d1:migrate": "wrangler d1 migrations apply GREENBRO_DB",
    "d1:console": "wrangler d1 execute GREENBRO_DB --command \"SELECT name FROM sqlite_schema WHERE type='table' AND name NOT LIKE 'sqlite_%' ORDER BY 1;\""
  },
  "dependencies": {
    "jose": "5.9.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251011.0",
    "typescript": "5.6.3",
    "wrangler": "^4.45.0"
  }
}
