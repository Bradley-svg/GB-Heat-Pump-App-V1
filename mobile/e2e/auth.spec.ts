describe.skip("Auth flow placeholder", () => {
  it("signs in with valid credentials (to be implemented)", async () => {
    // This spec exists to keep Detox wiring in place. Once test accounts are available,
    // remove the skip and exercise the login screen.
  });
});
