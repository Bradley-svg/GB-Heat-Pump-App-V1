# Mode A Dashboard (Web)

Vite + React SPA consuming the overseas API via `@greenbro/sdk-web` and the shared UI library. Defaults to sample data if the API base (`VITE_APP_API_BASE`) is not configured.

```
pnpm --filter @greenbro/dashboard-web dev
```
