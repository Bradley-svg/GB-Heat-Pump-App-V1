# Mode A Mobile App

Expo-based mobile shell demonstrating how `@greenbro/sdk-rn` and `@greenbro/ui-rn` compose to deliver pseudonymized telemetry to operators.

```
pnpm --filter @greenbro/mobile start
```

Set `EXPO_PUBLIC_APP_API_BASE` for real data; otherwise the bundled snapshot renders in offline mode.
